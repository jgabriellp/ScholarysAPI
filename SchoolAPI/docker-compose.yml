version: "3.9"

services:
  postgresdb:
    image: postgres:14-alpine
    container_name: postgres_escola_db
    environment:
      POSTGRES_USER: admin_escola_db
      POSTGRES_PASSWORD: "1q2w3e4r@#$"
      POSTGRES_DB: escola_db
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
